import javax.sql.DataSource;
import org.apache.commons.dbcp2.BasicDataSource;
import org.springframework.beans.factory.annotation.Value;
import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;
import org.springframework.context.annotation.PropertySource;
import org.springframework.jdbc.datasource.DataSourceTransactionManager;
import org.springframework.transaction.PlatformTransactionManager;
import org.springframework.transaction.annotation.EnableTransactionManagement;
import org.springframework.transaction.annotation.TransactionManagementConfigurer;
spring.devtools.livereload.enabled=true
spring.freemaker.cache=false;
server.port=8888
spring.mvc.view.prefix=/WEB-INF/views/
spring.mvc.view.suffix=.jsp

#로그 남기는 루트 레벨
logging.level.root = info


#Mysql 연결하는 법
spring.data.source.driver-Class-name = com.mysql.jdbc.Driver
spring.datasource.url=jdbc:mysql://localhost:3306/imin?serverTimezone=UTC&allowPublicKeyRetrieval=true&useSSL=false
spring.datasource.username=root
spring.datasource.password=dudtjr1!

CREATE TABLE USER (
    'id' int(11) unsigned NOT NULL AUTO_INCREMENT COMMENT 'Id',
    'token' varchar(100) DEFAULT NULL COMMENT 'user token',
    'os_type' varchar(100) DEFAULT NULL COMMENT 'os_type',
    'is_push' int(11) unsigned NOT NULL COMMENT '푸시 수락 여부',
    'created_at' datetime DEFAULT NULL COMMENT '생성 일자',
    'updated_at' datetime DEFAULT NULL COMMENT '수정 일자',
    PRIMARY KEY ('id')
) ENGINE=InnoDB DEFAULT CHARSET = utf8

CREATE TABLE USER ( 'uid' int(11) unsigned NOT NULL AUTO_INCREMENT COMMENT 'Id',
'token' varchar(100) DEFAULT NULL COMMENT 'user token',
'os_type' varchar(100) DEFAULT NULL COMMENT 'os_type',
'is_push' int(11) unsigned NOT NULL COMMENT '푸시 수락 여부',
'created_at' datetime DEFAULT NULL COMMENT '생성 일자',
'updated_at' datetime DEFAULT NULL COMMENT '수정 일자',
PRIMARY KEY ('id')
) ENGINE=InnoDB DEFAULT CHARSET = utf8;
